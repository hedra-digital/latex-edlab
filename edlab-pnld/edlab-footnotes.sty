% edlab-footnotes.sty
\RequirePackage[para*]{manyfoot}
\RequirePackage[stable,bottom,multiple]{footmisc}



% Vertical space between paragraphs in multiparagraph footnotes
\renewcommand{\hangfootparskip}{3pt plus 3pt minus 3pt}

% Setting left margin; this is the smallest value I can get to have 
% second etc. lines indented to footnote number; 
% zero put indentation to some positive value, and negative values do not help actually
\renewcommand{\footnotemargin}{0.00001pt}

% Indentation for 2nd etc. paragraphs in footnotes which cosists of more than one paragraph
\renewcommand{\hangfootparindent}{1ex}

% Here you can modify the spacing between the footnote 
% number and the text of footnote; keep this value and \hangfootparindent value the same
\renewcommand{\footnotelayout}{\hspace{1ex}}


% Notas de fim (package: \usepackage{endnotes})

\DeclareOption{endnotes}{
\AtEndOfClass{\RequirePackage{endnotes}
\def\notesname{Notas}
\def\enoteheading{\chapter{\notesname}}%
\let\footnote\endnote}
}

\DeclareOption{notasemlinha}{\AtBeginDocument{\renewcommand{\footnoterule}{\vfill
  \kern-3\p@
  \hrule width .4\columnwidth height 0pt
  \kern 2.6\p@}}}

\DeclareOption{notalinhalonga}{\AtBeginDocument{\renewcommand{\footnoterule}{\vfill
  \kern-3\p@
  \hrule width \textwidth height .2pt
  \kern 2.6\p@}}}

\DeclareOption{chicagofootnotes}{
\renewcommand*\footnoterule{\vfill}
  \renewcommand\@makefntext[1]{\frenchspacing 
    \parindent 0em% 
    \@thefnmark.~#1}
    % endnotes
    \newcommand\enoteformat{\parindent 1em\theenmark.~}
    }

     %reset das notas
  \pretocmd{\@schapter}{\setcounter{footnote}{0}}{}{}
  \pretocmd{\@chapter}{\setcounter{footnote}{0}}{}{}    

\ProcessOptions%


